apiVersion: v1
kind: Service
metadata:
  name: {{ include "proxmox-operator.fullname" . }}
  labels:
    {{- include "proxmox-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "proxmox-operator.selectorLabels" . | nindent 4 }}
